import { A } from '../theme'
export default function DashboardPage() {
  return <div style={{ padding: 40, textAlign: 'center', color: A.textSoft }}>Dashboard — coming in feature/react-dashboard</div>
}
