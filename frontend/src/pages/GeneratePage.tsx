import { A } from '../theme'
export default function GeneratePage() {
  return <div style={{ padding: 40, textAlign: 'center', color: A.textSoft }}>Content generator — coming in feature/streaming-ui</div>
}
